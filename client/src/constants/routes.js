// Public routes
const HOME = '/';
const EXAMS = '/Exams';

// Post routes

const CREATEEXAMS = '/CreateExams';
const DISPLAYEXAM = '/DisplayExam';
const DISPLAYPATIENT = '/Patient';
const ADMIN = '/Admin';

export const routes = {
  HOME,
  EXAMS,
  CREATEEXAMS,
  DISPLAYEXAM,
  DISPLAYPATIENT,
  ADMIN,
};
